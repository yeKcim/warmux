#ifndef __IMG_SAVE_H__
#define __IMG_SAVE_H__

#include <SDL.h>

int IMG_SavePNG32(SDL_Surface* img, const char *name);
int IMG_SavePPM24(SDL_Surface* img, const char *name);
SDL_Surface* IMG_LoadWithFormat(const char* name, SDL_PixelFormat* fmt);
SDL_Surface* IMG_LoadSprite(const char* name, SDL_PixelFormat* fmt);

#endif // __IMG_SAVE_H__
