FILE(GLOB_RECURSE WORMUX_SERVER_SRCS src/*.cpp)

INCLUDE_DIRECTORIES( ${CMAKE_CURRENT_SOURCE_DIR}/include )
LINK_DIRECTORIES( ${PROJECT_SOURCE_DIR}/lib/wormux )

ADD_LIBRARY( wormux-server STATIC ${WORMUX_SERVER_SRCS} )
TARGET_LINK_LIBRARIES( libwormux )
