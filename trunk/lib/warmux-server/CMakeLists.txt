FILE(GLOB_RECURSE WARMUX_SERVER_SRCS src/*.cpp)

INCLUDE_DIRECTORIES( ${CMAKE_CURRENT_SOURCE_DIR}/include )
LINK_DIRECTORIES( ${PROJECT_SOURCE_DIR}/lib/warmux )

ADD_LIBRARY( warmux-server STATIC ${WARMUX_SERVER_SRCS} )
TARGET_LINK_LIBRARIES( libwarmux )
